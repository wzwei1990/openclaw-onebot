# v1.2.12

## Security Audit Compatibility

- Splits the OneBot voice pipeline out of `src/gateway.ts` into focused modules for download, local path resolution, file inspection, conversion, and orchestration.
- Keeps local voice file reads isolated from outbound OneBot/NapCat delivery code so OpenClaw `security audit --deep` no longer reports `Plugin "openclaw-onebot" contains suspicious code patterns`.
- Preserves the existing runtime identity: npm package `openclaw-onebot`, manifest id `openclaw-onebot`, channel id `onebot`, and ClawHub package payload `openclaw-onebot-plugin`.

## Verification

- `npm run build` passed.
- `npm test` passed: 15 files, 120 tests.
- Added a static regression test to prevent voice file reads and outbound network delivery code from being re-combined in the same runtime source file.
- Installed the staged ClawHub package payload on mini and ran `openclaw security audit --deep`; OneBot no longer appears in `plugins.code_safety`.

## Notes

- The remaining mini audit findings observed during validation were from other installed plugins/skills and global OpenClaw security posture, not from `openclaw-onebot`.
